# Target for the dev-server proxy (see vite.config.ts). Only used by `bun run dev` -
# the production build has no env vars, it's served by the API on the same origin.
VITE_API_PROXY_TARGET=http://localhost:3000
